defmodule Sconely.EmailTemplateViewTest do
  use SconeHomeElixir.ConnCase, async: true

  import Phoenix.View

  #setup_all
  #setup
  #on_exit

  test "complete order email" do

  	#assert render_to_string(Sconely.SconelyHomepageView, "sconely_social_complete_order_email.html", []) == ""

    #assert render_to_string(Sconely.SconelySocialCompleteOrderEmailView, "sconely_social_complete_order_email.html", []) == ""
  end
end
